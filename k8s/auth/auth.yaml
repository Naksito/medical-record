# Auth Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: medical-record
  labels:
    app: auth-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: __AUTH_IMAGE__:__AUTH_TAG__
          imagePullPolicy: Always
          ports:
            - containerPort: 9091
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          env:
            - name: SERVICE_VERSION
              value: "__AUTH_TAG__"
            - name: AUTH_PORT
              value: "9091"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth-secret
                  key: JWT_SECRET
            - name: USERS_PORT
              value: "9090"
            - name: USERS_HOST
              value: "users-service"
            - name: ADMIN_EMAIL
              value: "admin"
            - name: ADMIN_PASSWORD
              value: "adminpass"
          livenessProbe:
            grpc:
              port: 9091
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            grpc:
              port: 9091
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: regcred

---

# Auth Service
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medical-record
spec:
  selector:
    app: auth-service
  ports:
    - port: 9091
      targetPort: 9091
  type: ClusterIP