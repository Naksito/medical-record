name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: maven-${{ runner.os }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-${{ runner.os }}-
      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push auth image
        run: |
          docker build -t naksito03/auth:latest .
          docker push naksito03/auth:latest

      - name: Build and push api image
        run: |
          docker build -t naksito03/api:latest .
          docker push naksito03/api:latest      

      - name: Build & push users image
        run: |
          cd users
          docker build -t naksito03/users-service:latest .
          docker push naksito03/users-service:latest

      - name: Build & push appointments image
        run: |
          cd appointments
          docker build -t naksito03/appointments-service:latest .
          docker push naksito03/appointments-service:latest